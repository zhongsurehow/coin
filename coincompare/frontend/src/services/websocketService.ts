// Placeholder for websocketService.ts
class WebsocketService {
  onConnectionChange(callback: (status: string) => void) {
    //
  }
  onNotification(callback: (notification: any) => void) {
    //
  }
  connect() {
    //
  }
  disconnect() {
    //
  }
}

export const websocketService = new WebsocketService();
